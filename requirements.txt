# requirements.txt — versions épinglées
# Mettre à jour manuellement après tests : pip install package==X.Y.Z

# Core
crewai==1.14.1
crewai-tools==1.14.1
anthropic==0.73.0

# Interface
streamlit==1.56.0

# Recherche
tavily-python==0.7.23
feedparser>=6.0.0       # RSSFeedTool — pas de version exacte car optionnel

# Utilitaires
python-dotenv==1.1.1
requests==2.32.5

# Optionnel
chromadb==1.1.1         # CrewAI Memory (use_memory=True)
reportlab==4.4.10       # Export PDF rapport
